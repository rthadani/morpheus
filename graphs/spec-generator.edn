;; Wiggum spec generator
;;
;; Takes a description and produces a filled implementation spec in EDN
;; format, ready to run as its own Wiggum loop.
;;
;; Usage:
;;   1. Set :description below. One line for SIMPLE/MEDIUM, a paragraph or
;;      more for COMPLEX. Optionally append an agent/provider keyword:
;;        :ollama  :kimi  :minimax  :anthropic (default)   ; routed through claude CLI
;;        :pi                                              ; routed through pi CLI
;;      Examples:
;;        'A CLI tool that watches a directory and prints new file summaries'
;;        'An HTMX todo app in Clojure with SQLite :ollama'
;;        'A Kafka consumer lag monitor dashboard :kimi'
;;        'A Clojure TUI options-trading research workbench backed by
;;         IBKR + DuckDB + Claude Code as agent (see options-trader.edn). :pi'
;;   2. Provider env vars (must be set if non-default):
;;        :kimi    MOONSHOT_API_KEY
;;        :minimax MINIMAX_API_KEY
;;        :ollama  none — needs `ollama` CLI on PATH and the model pulled
;;        :pi      routed via the pi CLI. Executor is always :kimi
;;                 (kimi-k2.5); supervisor varies by complexity:
;;                 SIMPLE→:deepseek, MEDIUM→:kimi, COMPLEX→:minimax.
;;                 MOONSHOT_API_KEY is always required; SIMPLE also
;;                 needs DEEPSEEK_API_KEY, COMPLEX also needs
;;                 MINIMAX_API_KEY. Needs `pi` CLI on PATH.
;;   3. Run: clj -M:run graphs/spec-generator.edn --project-dir ./spec-out
;;   4. Output: spec-out/output/<slug>.edn — review, then run it.

{:description
 "REPLACE THIS with a one-line description for SIMPLE/MEDIUM, or a full
  paragraph (multiple sentences, target stack, scope hints) for COMPLEX.
  Optionally end with a provider/agent keyword (:ollama :kimi :minimax
  :anthropic :pi). The generator strips the keyword from the slug/objective
  but uses it for model selection."

 :objective
 "You are a spec writer for a Clojure-based agent build system called Wiggum.
  Read the description and produce a single output EDN file that Wiggum can
  run directly to build the described system.

  Description:
  {{description}}

  === Phase 1: Analyse the description ===

  Write your analysis to STDOUT before touching any file.  Cover:

  1. Core deliverable — what gets built, what it does
  2. Inferred stack — language, frameworks, persistence; justify each choice
  3. Complexity classification — pick exactly one:
       SIMPLE   single script or utility, 1-2 files, no server, no persistence
       MEDIUM   full-stack app or service, 2-4 phases, standard persistence
       COMPLEX  multi-service system, 5+ phases, distributed concerns, architecture decisions
  4. Provider — scan the description for a trailing provider/agent keyword:
       :ollama      local inference via Ollama (`ollama launch claude --model ...`).
                    Routed through the claude CLI.
       :kimi        Moonshot via Anthropic-compatible endpoint
                    (api.moonshot.ai/anthropic). Requires MOONSHOT_API_KEY.
                    Routed through the claude CLI.
       :minimax     MiniMax via Anthropic-compatible endpoint
                    (api.minimax.chat/anthropic). Requires MINIMAX_API_KEY.
                    Routed through the claude CLI.
       :anthropic   Anthropic Claude API (default if no keyword present).
                    Routed through the claude CLI.
       :pi          Routed through the pi CLI instead of claude.
                    Executor is always :kimi (kimi-k2.5); supervisor
                    varies by complexity (see Phase 2):
                      SIMPLE  → supervisor :deepseek (DEEPSEEK_API_KEY)
                      MEDIUM  → supervisor :kimi     (MOONSHOT_API_KEY)
                      COMPLEX → supervisor :minimax  (MINIMAX_API_KEY)
                    MOONSHOT_API_KEY is always required (executor);
                    supervisor's env var depends on complexity.
     Strip the keyword from the description before using it as the
     objective title or slug, but record it for model selection.
     If the chosen provider needs an env var, mention it in the analysis
     output so the user knows to export it before running the generated spec.
  5. Judge mode for the output spec — pick exactly one:
       :code      Software build. The deliverable is source code, tests,
                  configuration, deploy artifacts. The downstream judge
                  scores file-scope discipline, slop, hallucinated APIs.
                  Default for almost everything: web apps, services, CLIs,
                  libraries, data pipelines, dashboards, TUIs, MCP servers.
       :research  Fetch/read/synthesise content. The deliverable is
                  written analysis with citations: market briefings,
                  literature reviews, due-diligence reports, competitive
                  scans, summarisation runs over corpora. The downstream
                  judge scores citation discipline, faithfulness to
                  source, source diversity, recency, coverage.
     Pick by what the downstream run actually produces. A 'data analysis
     dashboard' is :code (the deliverable is the dashboard). A 'research
     report on the EV battery supply chain' is :research (the deliverable
     is the writeup).

     Also pick a :review-threshold that fits the work:
       :none    skip the judge entirely (no LLM call, no pauses). Use
                only when the user has explicitly asked for no review.
       :low     pause on any violation
       :medium  pause on medium or high
       :high    pause on high only
     Defaults: :high for :code, :medium for :research. Pick :none only
     when the user explicitly wants the run to grind through without
     stopping for quality review.

  6. Proposed phases and order.  Each phase header must open with a
     `Deliverables:` line listing the concrete files (or, for a phase that
     fleshes out an existing file, the routes/functions added).  The
     downstream supervisor lifts these directly into per-iteration
     `expected_files` — vague phase headers degrade to vague narrowing.
  7. Proposed :success-check command — single shell chain, file existence
     plus the project's native test runner.  No live smoke-tests, no
     setsid, no fuser, no docker build (see :success-check section in
     Phase 3).  Pick the test invocation idiomatic to the chosen stack;
     do not prescribe one — you already know how the language tests.
  8. Ambiguities and how you resolved them

  === Phase 2: Choose models ===

  Based on complexity classification and provider, select models for all four
  model fields in the output spec.  Both :executor-model-config and
  :supervisor-model-config are MAPS, never bare strings — emitting
  `:executor-model-config 'claude-sonnet-4-6'` is wrong and the judge will
  fail the spec.  Use this table:

  ┌────────────┬──────────┬─────────────────────────────────────────────────────────┬───────────────────────────────────────────────────────────┐
  │ Provider   │ Complex  │ :executor-model-config                                  │ :supervisor-model-config                                  │
  ├────────────┼──────────┼─────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────┤
  │ :anthropic │ SIMPLE   │ {:model-id 'claude-haiku-4-5-20251001'}                 │ {:model-id 'claude-sonnet-4-6'}                           │
  │ :anthropic │ MEDIUM   │ {:model-id 'claude-sonnet-4-6'}                         │ {:model-id 'claude-sonnet-4-6'}                           │
  │ :anthropic │ COMPLEX  │ {:model-id 'claude-sonnet-4-6'}                         │ {:model-id 'claude-opus-4-7'}                             │
  ├────────────┼──────────┼─────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────┤
  │ :ollama    │ SIMPLE   │ {:provider :ollama :model-id 'qwen2.5-coder:7b'}        │ {:provider :ollama :model-id 'qwen2.5-coder:14b'}         │
  │ :ollama    │ MEDIUM   │ {:provider :ollama :model-id 'qwen2.5-coder:14b'}       │ {:provider :ollama :model-id 'qwen2.5-coder:32b'}         │
  │ :ollama    │ COMPLEX  │ {:provider :ollama :model-id 'qwen2.5-coder:32b'}       │ {:provider :ollama :model-id 'qwen2.5-coder:32b'}         │
  ├────────────┼──────────┼─────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────┤
  │ :kimi      │ ANY      │ {:provider :kimi :model-id 'kimi-k2.5'}                 │ {:provider :kimi :model-id 'kimi-k2.5'}                   │
  │ :minimax   │ ANY      │ {:provider :minimax :model-id 'MiniMax-M2.7'}           │ {:provider :minimax :model-id 'MiniMax-M2.7'}             │
  ├────────────┼──────────┼─────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────┤
  │ :pi        │ SIMPLE   │ {:agent :pi :provider :moonshotai :model-id 'kimi-k2.5'}│ {:agent :pi :provider :deepseek :model-id 'deepseek-chat'}│
  │ :pi        │ MEDIUM   │ {:agent :pi :provider :moonshotai :model-id 'kimi-k2.6'}│ {:agent :pi :provider :minimax :model-id 'MiniMax-M2.7'}  │
  │ :pi        │ COMPLEX  │ {:agent :pi :provider :moonshotai :model-id 'kimi-k2.6'}│ {:agent :pi :provider :zai :model-id 'glm-5.2'}           │
  └────────────┴──────────┴─────────────────────────────────────────────────────────┴───────────────────────────────────────────────────────────┘

  The Anthropic-compatible providers (:kimi :minimax) don't publish a
  meaningful size ladder we can map to complexity — pick the flagship
  model the provider exposes via its Anthropic-compat layer.

  The :pi rows mirror the :anthropic ladder. The EXECUTOR is pinned to
  :kimi across all three complexities (kimi-k2.5 is the workhorse coder
  model), exactly the way :anthropic pins the executor to
  claude-sonnet-4-6 for MEDIUM and COMPLEX. Only the SUPERVISOR varies
  with complexity:
    SIMPLE  → :deepseek (cheap reviewer)
    MEDIUM  → :kimi     (same as executor)
    COMPLEX → :minimax  (strongest reviewer for the hardest plans)

  Pi keeps its own provider list (:google :openai :anthropic :kimi
  :minimax :ollama :deepseek …); the dispatch passes `--provider` and
  `--model` through verbatim, so unknown pi providers fall back to pi's
  default routing. Credentials are pi's responsibility — set whatever
  env vars the chosen pi provider needs. With executor always :kimi,
  MOONSHOT_API_KEY is always required for pi runs; SIMPLE additionally
  needs DEEPSEEK_API_KEY and COMPLEX additionally needs MINIMAX_API_KEY
  for the supervisor.

  Also set :fallback-model:
    :anthropic → 'claude-haiku-4-5-20251001'
    :ollama    → 'qwen2.5-coder:7b'
    :kimi      → 'kimi-k2.5'
    :minimax   → 'MiniMax-M2.7'
    :pi        → omit :fallback-model and :fallback-delay-ms (no
                 automatic fallback is wired for the pi agent; the run
                 surfaces rate-limit exhaustion to the user instead)

  Model-config shape — keep it minimal:
    :anthropic   {:model-id 'claude-sonnet-4-6'}
    :ollama      {:provider :ollama :model-id 'qwen2.5-coder:14b'}
    :kimi        {:provider :kimi    :model-id 'kimi-k2.5'}
    :minimax     {:provider :minimax :model-id 'MiniMax-M2.7'}
    :pi          {:agent :pi :provider <pi-provider-kw> :model-id '<pi-model-id>'}
                 ;; :provider is required — pick :deepseek / :kimi /
                 ;; :minimax per the complexity row above. The :agent :pi
                 ;; key is what routes the run through the pi CLI instead
                 ;; of claude.

  Provider routing is dispatched in
  `src/morpheus/executor/claude_code_agent.clj/build-cmd+env`
  (for the claude agent) and `src/morpheus/executor/pi_agent.clj`
  (for the pi agent). Only the keys those dispatch functions actually
  read are honoured — env-var names (MOONSHOT_API_KEY, MINIMAX_API_KEY)
  are hardcoded per provider, base-urls default to the provider's
  Anthropic-compat endpoint, and there is no :auth-token-env key.

  Do NOT emit any of these dead keys — morpheus does not read them and
  the post-edit validation will flag them:
    :temperature, :max-tokens   — never threaded to either CLI
    :auth-token-env             — env-var names are hardcoded per provider
    :base-url                   — only override when targeting a non-default
                                   endpoint for :kimi / :minimax; omit otherwise

  Also set :max-iterations from complexity:
    SIMPLE  → 8
    MEDIUM  → 20
    COMPLEX → 35

  For COMPLEX, also set :checkpoint-every to ceil(max-iterations / 5)
  so the human gets a periodic review pause on long runs. Omit the key
  for SIMPLE/MEDIUM.

  === Phase 3: Write the spec file ===

  Create output/ if absent.  Write output/<slug>.edn where <slug> is a
  kebab-case identifier derived from the description (provider keyword excluded).

  --- Depth scaling (CRITICAL) ---

  Scale the spec's depth to match complexity:

    SIMPLE  ~50-150 lines. One-screen objective with a few bullet points
            per phase. No :acceptance-criteria. The Kanban example below
            is the model.

    MEDIUM  ~150-500 lines. Each phase enumerates concrete files,
            functions, routes, and tests. :acceptance-criteria required
            with one shell-runnable check per phase.

    COMPLEX ~1000-4000 lines. Each phase enumerates every namespace,
            every public function, every database column, every tool
            schema, every test fixture. Constraints and anti-goals are
            domain-specific and exhaustive (15-25 of each is normal,
            not too many). :acceptance-criteria is mandatory and must
            include real shell commands gating phase advancement against
            real data — not just `clojure -M:test`. The reference for
            COMPLEX is `graphs/options-trader/options-trader.edn` in
            this repo: read it before writing a COMPLEX spec. Match its
            level of detail; do not produce a thin sketch and call it
            done.

  Under-detailing a COMPLEX spec is a defect. The supervisor will run
  fewer iterations against a thin spec because each iteration's
  `expected_files` will be vague, and the judge can't flag scope
  violations against expected_files that don't exist.

  --- Output schema ---

  The file must be a valid EDN map with exactly these keys in this order:

    :objective          — what to build, phase by phase.
                          Use === Phase N: name === headings.

                          Each phase header MUST be followed by a
                          `Deliverables:` block listing concrete files
                          (one per line) or, for phases that flesh out
                          an existing file, the specific routes/functions
                          added.  The supervisor lifts this list straight
                          into per-iteration `expected_files`, so vague
                          headers (no Deliverables: block, or prose-only
                          deliverables like 'a working server') produce
                          vague narrowing and lots of wasted iterations.

                          Be specific: file names, API routes, data shapes,
                          test expectations.

                          Context-window budget — HARD CAPS:
                            Whole :objective body must stay under 12000
                              characters (roughly 3000 tokens).  The
                              executor's iteration-1 CLAUDE.md embeds the
                              FULL :objective verbatim, and the supervisor
                              re-reads the original objective on every
                              iteration to plan narrowing.  An overlong
                              objective hijacks the context budget the
                              executor needs for the actual file bodies it
                              writes, and bloats the supervisor's prompt
                              every turn.
                            Per-phase body must stay under ~2000 chars.
                              If a phase needs more detail than that fits,
                              you have two options:
                                (a) describe the structure in the phase
                                    body (file list, function signatures,
                                    schema names) and let the executor
                                    fill in the bodies — Claude knows the
                                    idioms of every common stack, so do
                                    not re-teach them.
                                (b) lift the detail into a Phase-1
                                    deliverable file (`docs/phase-N.md`,
                                    `mockup.html`, `schema.sql`) that
                                    later phases reference.  Visual
                                    phases (mockup.html / mockup.txt /
                                    expected-session.txt) are the
                                    canonical example.
                            Never inline a verbatim file body longer than
                              ~50 lines.  Lift it to a Phase-1 deliverable
                              and reference it.  This is the most common
                              cause of bloat in generated specs.
                            Phase 4 validation rejects any spec whose
                              :objective exceeds the cap.

                          UI handling — pick the visual artifact based on
                          the kind of UI:
                            web/HTML        → output/mockup.html
                            terminal/TUI    → output/mockup.txt (ASCII
                                              layout boxes, like the panes
                                              shown in options-trader.edn
                                              Phase 7)
                            CLI w/ output   → output/expected-session.txt
                                              (sample command + expected
                                              stdout transcript)
                            none (library,
                            pipeline)       → skip the visual phase entirely

                          When a visual phase is needed, insert it AFTER
                          the skeleton phase and BEFORE any phase that
                          implements the UI:

                            === Phase N: Visual reference ===
                            Produce <output/mockup.html | mockup.txt |
                            expected-session.txt>. The artifact must
                            specify the full layout, components, and
                            interaction states. Subsequent phases must
                            match it exactly — no design values
                            (CSS, ASCII layout, transcript text) restated
                            elsewhere.

                          The visual artifact is REFERENCE, NOT THE
                          DELIVERABLE.  This rule is universal — applies to
                          mockup.html, mockup.txt, and expected-session.txt
                          equally.  Every UI-implementation phase must say:
                            * mockup.<ext> specifies layout, components,
                              and interaction states only.
                            * The implementation phase MUST re-express the
                              mockup in the idiomatic primitives of the
                              chosen stack — never serve / paste / copy
                              the mockup file verbatim as the running UI.
                            * Name the idiomatic primitive in the phase
                              body so the executor cannot fall back to
                              static-file shortcuts.  Examples:
                                HTMX (server-rendered)
                                  → server routes returning HTML
                                    fragments via hiccup / Jinja /
                                    ERB / html/template, with `hx-get`,
                                    `hx-post`, `hx-target`, `hx-swap`,
                                    `hx-swap-oob`, and `hx-ext=\"sse\"`
                                    on the elements that the mockup
                                    shows as interactive.
                                React / Vue / Svelte
                                  → components + state hooks + routes;
                                    the mockup's static HTML becomes a
                                    tree of components, not innerHTML.
                                iOS (SwiftUI / UIKit)
                                  → SwiftUI Views or UIViewControllers;
                                    mockups translate to View structs
                                    and @State / @Binding, not
                                    WKWebView-hosted HTML.
                                Android (Jetpack Compose / Views)
                                  → @Composable functions or XML layouts
                                    + ViewModels, not WebView-hosted
                                    HTML.
                                React Native / Flutter
                                  → platform widgets (View/Text,
                                    Widget tree); mockup → widget tree,
                                    not embedded HTML.
                                TUI
                                  → terminal-widget library of the
                                    chosen stack (jline, blessed,
                                    ratatui, urwid); mockup.txt is the
                                    layout target, not the rendered
                                    output.
                                CLI
                                  → argument parser + formatter
                                    producing the expected-session.txt
                                    transcript shape.
                            * Acceptance checks must prove the
                              re-implementation happened, not just that
                              the mockup file exists.  Pick a grep that
                              would fail on a verbatim copy — e.g. grep
                              for `hx-get` in served HTML for HTMX,
                              `@Composable` for Compose, `struct .*: View`
                              for SwiftUI, JSX tags for React.

                          Live updates rule (universal) — whenever the
                          description calls for streaming, progress,
                          notifications, real-time, or any push behavior,
                          the implementation phase MUST use server-push,
                          not client polling.  Name the idiomatic
                          primitive in the phase body:
                            HTMX                → `hx-ext=\"sse\"` +
                                                  `sse-connect=\"/...\"` +
                                                  named server events;
                                                  the server route returns
                                                  `text/event-stream`.
                            React / Vue / Svelte → `EventSource` (or
                                                  WebSocket only if the
                                                  channel is bidirectional)
                                                  — never `setInterval`
                                                  fetch loops.
                            iOS                 → URLSession streaming
                                                  task / `bytes(for:)`
                                                  consuming SSE, or a
                                                  WebSocket task.
                            Android             → OkHttp `EventSource`
                                                  (okhttp-sse), or Ktor /
                                                  Retrofit streaming with
                                                  Kotlin `Flow`.
                            React Native        → `react-native-sse`.
                            Flutter             → `dart:io` HttpClient
                                                  with SSE parsing, or a
                                                  WebSocket.
                            CLI / TUI           → stream stdout / stdin
                                                  pipe; no `sleep`-based
                                                  re-fetch loops.
                          Server side, name the streaming primitive too —
                          http-kit `with-channel` / Ring SSE, FastAPI
                          `EventSourceResponse`, Rails
                          `ActionController::Live`, Spring `SseEmitter`,
                          Node `res.write` on a `text/event-stream`
                          response, Go `http.Flusher`.
                          Acceptance check must fail on the polling
                          shortcut — e.g. grep the client for
                          `setInterval(` / `setTimeout(` around fetch
                          calls and exit non-zero if found, or grep the
                          server for `text/event-stream` and require it
                          present.

                          Styling rule (universal) — whenever the project
                          renders a visual UI (web / mobile / desktop),
                          Tailwind is the default styling system.  The
                          implementation phase MUST:
                            * Pull Tailwind in via the stack's idiomatic
                              integration — DO NOT handwrite a
                              `style.css` of bespoke class rules or
                              inline CSS-in-JS sheets when Tailwind
                              suffices:
                                HTMX / server-rendered HTML
                                  → Tailwind Play CDN for quick projects,
                                    or `@tailwindcss/cli` build for real
                                    ones.  Use `@layer components` +
                                    `@apply` only for repeated multi-utility
                                    bundles, never to recreate Bootstrap.
                                React / Vue / Svelte
                                  → `tailwindcss` postcss plugin (Vite /
                                    Next / Nuxt / SvelteKit preset);
                                    utilities go on JSX/template
                                    attributes directly.
                                React Native
                                  → `nativewind` (Tailwind class names
                                    on `<View>` / `<Text>` props).
                                Flutter
                                  → `tailwind_native` or skip — Flutter
                                    has its own idiomatic styling; do
                                    not force Tailwind where it does
                                    not fit, but state the alternative
                                    explicitly in the phase.
                                iOS / Android native (SwiftUI /
                                  Compose)
                                  → skip Tailwind — use the platform's
                                    native styling (SwiftUI modifiers,
                                    Compose Modifier chain); state this
                                    explicitly in the phase so the
                                    executor doesn't try to bolt it on.
                            * Use the design tokens (`bg-*`, `text-*`,
                              spacing, etc.) from the mockup — the
                              mockup's static HTML should already carry
                              Tailwind classes the implementation can
                              port verbatim.
                            * Theming (light / dark) uses Tailwind's
                              `dark:` variant + a `data-theme`
                              attribute, not hand-rolled
                              `prefers-color-scheme` media queries.
                            * Acceptance check: grep the rendered output
                              (or component source) for Tailwind utility
                              patterns (e.g. `class=\"[^\"]*\\b(flex|grid|p-\\d|text-)`)
                              and fail if absent.  For native UIs
                              (SwiftUI / Compose / Flutter), the grep is
                              for the platform's modifier idiom
                              instead — pick one and state it.

                          Behaviour-verification rule (universal) — verify
                          observable behaviour, not artifacts.  This is the
                          single biggest cause of a run finishing at ~90%:
                          the success-check proves files exist and unit tests
                          pass, so whatever the check cannot SEE is where the
                          executor stops, and the human plugs the last 10% by
                          hand.  Whenever a feature's correctness is
                          user-observable AND a unit-test DOM (jsdom / RTL /
                          Vitest / jest-dom) cannot faithfully reproduce it, a
                          passing unit test is FALSE confidence.  This covers:
                          drag-and-drop, focus / selection / scroll, canvas /
                          WebGL, clipboard, file upload/download, keyboard
                          navigation, animations, and any frontend<->backend
                          round-trip whose only real proof is `the data is
                          still there after reload`.
                          For these the implementing phase MUST add a real
                          end-to-end test that drives the RUNNING system and
                          asserts the observable outcome, and that e2e MUST be
                          wired into :success-check so the executor actually
                          gets the signal each iteration:
                            web           → Playwright (`@playwright/test`)
                                            with a `webServer` config that
                                            boots the app, and a `test(` that
                                            performs the real interaction
                                            (e.g. `page.dragAndDrop(...)`),
                                            asserts the outcome, then reloads
                                            to prove persistence.
                            iOS / Android → XCUITest / Espresso (or Maestro).
                            React Native  → Detox / Maestro.
                            Flutter       → integration_test.
                            CLI / TUI     → spawn the built binary and assert
                                            on real stdout, not a mocked pipe.
                          A Playwright (or device) e2e is NOT the banned
                          `live smoke-test` — its framework starts and stops
                          the app deterministically, unlike ad-hoc curl /
                          port-probe / docker commands, so it is allowed (and
                          required) in :success-check despite the long-lived
                          process rule below.
                          Acceptance check must prove the e2e EXERCISES the
                          behaviour, never merely that an implementation token
                          is present.  Bad: grep for `draggable` or
                          `import .*dnd`.  Good: grep for `@playwright/test`
                          plus a `dragAndDrop`/`page.` interaction.  A
                          unit-test-only drag-and-drop (or any behaviour in
                          the list above) is a defect.

                          End with a Documentation phase covering README.md.

                          IMPORTANT — EDN string foot-guns inside this
                          string body:
                            * Embedded `\"` terminates the outer string.
                              For Clojure maps, JSON examples, Maven
                              coordinates: use single-quoted 'literals'
                              or backtick `code` style instead.
                              The options-trader.edn :tui block is the
                              model.
                            * Bare `\\` followed by a newline is NOT a
                              valid EDN escape — the strict reader fails
                              with `Unsupported escape character: \\`.
                              When showing a multi-line shell command
                              with line continuations, write `\\\\` (two
                              backslashes in the EDN source) so the parsed
                              string contains a single literal `\\`.
                            * Validate with `clojure.edn/read-string`
                              (strict) — `clojure.core/read-string` is
                              lenient and silently accepts both bugs.

    :project-dir        — nil

    :acceptance-criteria — string. REQUIRED for MEDIUM/COMPLEX, omit for
                          SIMPLE. Per-phase shell commands the supervisor
                          runs at the end of each iteration. Format:
                            --- Phase N (name) ---
                            Command:
                              <one shell command>
                            Required:
                              * <assertion the command exit/output proves>
                              * ...
                          The commands here MUST be more specific than
                          :success-check — :success-check verifies the
                          whole build at the end; :acceptance-criteria
                          gates one phase at a time.

    :success-check      — shell one-liner that exits 0 only when the
                          per-iteration deliverables are present AND the
                          project's tests pass.  Run by morpheus via
                          `sh -c <whole-string>`.
                          Hard rules:
                            Single command chain only — every line ends
                              with ` \\` continuation or ` && ` connector.
                              Trailing prose / Note: blocks / blank lines
                              followed by comment paragraphs are FORBIDDEN —
                              `sh -c` parses them as separate commands and
                              the exit code becomes the last prose line
                              (typically 127), so verification can never
                              report success.
                            Tests are MANDATORY, not optional — the chain
                              must invoke the project's native test runner
                              at the end of the chain (after file-existence
                              checks).  Pick the idiomatic command for the
                              chosen stack; this generator does not
                              prescribe a language.  File-existence checks
                              alone are NOT sufficient: the executor can
                              create a file with broken syntax that still
                              passes `test -f`, so without a test
                              invocation the verification is a smoke alarm
                              with the battery removed.
                            Per-iteration scope only — file existence
                              + tests.  Do NOT include `setsid`, `fuser`,
                              `nohup`, `kill $SERVER_PID`, port probes,
                              `curl` to localhost, `docker build`, or any
                              ad-hoc command that starts a long-lived process.
                              These are fragile across runs (port busy,
                              race with `sleep`, subprocess scoping under
                              `sh -c`) AND `setsid` / `fuser` do not exist
                              on macOS.
                              EXCEPTION: a real e2e framework that manages its
                              own app lifecycle (Playwright with `webServer`,
                              Detox, integration_test) is allowed AND required
                              when the Behaviour-verification rule applies —
                              it boots and tears down deterministically, unlike
                              the ad-hoc commands above.  Put it last in the
                              chain (e.g. `npm run test:e2e`).
                            Live server smoke-test, `docker build`, and
                              `docker run` belong in a manual Phase-N step
                              documented in the README — never in
                              :success-check.
                            Tests must not call real external services
                              (network, real cloud APIs, prod databases).
                          If a visual phase is present, include
                          `test -f output/<artifact>` in the chain.

    :constraints        — vector of strings.  Always include:
                            'Work phase by phase — do not start phase N+1
                             until phase N tests pass'
                            'Run the test suite at the end of every iteration'
                          If the project has any browser-only / interactive
                          behaviour (per the Behaviour-verification rule —
                          drag-and-drop, focus/scroll, canvas, clipboard,
                          upload, navigation, persistence round-trips), always
                          add a constraint naming the e2e as the proof, e.g.:
                            '<behaviour> is proven by the <Playwright/Detox/...>
                             e2e run in :success-check, not by unit tests; the
                             e2e command must self-install its browser/runtime
                             headless so the check is self-contained'
                          If a visual phase is involved, always add:
                            'Produce output/<artifact> in the visual phase
                             before writing any code in dependent phases'
                            'All phases after the visual phase must reference
                             output/<artifact> — do not restate design values
                             (CSS, ASCII layout, transcript) in the objective'
                          Add further stack-specific constraints.

    :anti-goals         — vector of strings.  Always include:
                            'Do not add dependencies not listed in the objective'
                            'Do not leave TODO comments or unimplemented stubs'
                          If the project has any browser-only / interactive
                          behaviour, always add:
                            'Do not verify <behaviour> with unit tests alone,
                             and do not mock the mechanism under test (e.g.
                             DataTransfer, the network) to make a unit test
                             pass — it is done only when the e2e passes'
                          If a visual phase is involved, always add:
                            'Do not describe UI design in prose in the
                             objective — the visual artifact is the specification'
                          Add domain-specific anti-goals.

    :max-iterations     — integer from Phase 2 model selection

    :checkpoint-every   — integer for COMPLEX runs only (max-iterations / 5
                          rounded up). Omit for SIMPLE/MEDIUM.

    :step-once?         — literal boolean `false` (never the string \"false\";
                          the wiggum executor reads it with (boolean ...) and
                          a string is truthy).

    :generate-claude-md? — literal boolean `false`.

    :judge-mode         — keyword from Phase 1 step 5: :code or :research.
                          Wires the downstream judge to the matching rubric.
                          Required on every output spec; do not omit.

    :review-threshold   — keyword from Phase 1 step 5. Single switch that
                          controls the judge:
                            :none    skip the judge entirely (no LLM call,
                                     no pause). Use when the run is short
                                     enough that judging adds no value, or
                                     when the user explicitly does not want
                                     reviews.
                            :low     pause on any violation
                            :medium  pause on medium or high
                            :high    pause on high only (default for :code)
                          Default to :high for :code, :medium for :research.
                          Required on every output spec — never omit.

                          Note: the older `:review?` boolean flag is no
                          longer required. `:review-threshold :none` is the
                          single way to disable the judge.

    :fallback-model     — string from Phase 2 model selection.
                          Omit entirely for :pi runs.

    :fallback-delay-ms  — 20000.
                          Omit entirely for :pi runs.

    :executor-model-config  — MAP from Phase 2, NOT a bare string. Shape:
                                {:model-id 'model-id-here'}
                              for Anthropic, plus :provider for
                              :ollama/:kimi/:minimax, or :agent :pi for
                              pi-routed configs. Do NOT emit :temperature,
                              :max-tokens, or :auth-token-env — they are
                              dead keys morpheus does not read.

                              WRONG (string value — the spec judge will
                              reject this and wiggum will crash with
                              (:model-id 'claude-sonnet-4-6') returning nil):
                                :executor-model-config 'claude-sonnet-4-6'

                              RIGHT (map value):
                                :executor-model-config {:model-id 'claude-sonnet-4-6'}

    :supervisor-model-config — same MAP shape as :executor-model-config.
                               Never a bare string.

  === Phase 4: Validate ===

  Use the exact <slug> the spec was written under. Do NOT use
  `ls output/*.edn | head -1` — that picks a stale file when prior
  generator runs have left specs behind.

  1. cat output/<slug>.edn to verify it looks correct
  2. clojure -e '(clojure.edn/read-string (slurp \"output/<slug>.edn\"))'
     — must exit 0. Use clojure.edn/read-string (strict) not bare
     read-string (lenient); strict catches embedded `\"` and other
     escape bugs the lenient reader silently allows.
  3. Required-key checks — all must pass:
       grep -q '^ :objective'           output/<slug>.edn
       grep -q '^ :success-check'       output/<slug>.edn
       grep -q '^ :constraints'         output/<slug>.edn
       grep -q '^ :anti-goals'          output/<slug>.edn
       grep -q '^ :max-iterations'      output/<slug>.edn
       grep -q '^ :step-once?'          output/<slug>.edn
       grep -q '^ :generate-claude-md?' output/<slug>.edn
       grep -q '^ :judge-mode'          output/<slug>.edn
       grep -q '^ :review-threshold'    output/<slug>.edn
       grep -qE ':judge-mode +:(code|research)' output/<slug>.edn        ; valid value
       grep -qE ':review-threshold +:(none|low|medium|high)' output/<slug>.edn
       grep -qE ':step-once\\? +(true|false)\\b' output/<slug>.edn       ; boolean, not string
       ;; model configs MUST be maps, never bare strings. The downstream
       ;; wiggum executor calls (:model-id model-config) on them — a string
       ;; value blows up.
       grep -qE '^ :executor-model-config +\\{'   output/<slug>.edn
       grep -qE '^ :supervisor-model-config +\\{' output/<slug>.edn
     For MEDIUM/COMPLEX add:
       grep -q '^ :acceptance-criteria' output/<slug>.edn
     For COMPLEX add:
       grep -q '^ :checkpoint-every'    output/<slug>.edn
  4. If description implies a UI, exactly one visual artifact must be
     declared:
       grep -qE 'output/(mockup\\.html|mockup\\.txt|expected-session\\.txt)' output/<slug>.edn
       grep -q 'Visual reference' output/<slug>.edn
  5. Anti-pattern checks — all must be absent:
       ! grep -q 'kill $SERVER_PID'            output/<slug>.edn
       ! grep -qE ':temperature|:max-tokens|:auth-token-env' output/<slug>.edn  ; dead keys
       ! grep -qE 'color: *#|border-radius: *[0-9]|font-size: *[0-9]' output/<slug>.edn
                                  ; tightened: only flag actual CSS values,
                                  ; not the substring 'color' in prose like
                                  ; 'colour palette' or 'colors:'.
       ! grep -q '<<<' output/<slug>.edn   ; placeholder text not stripped
       ! grep -qE 'setsid|fuser ' output/<slug>.edn
                                  ; Linux-only, fails on Darwin
       ! awk '/^ :success-check/,/^[^ ]/' output/<slug>.edn | grep -qE '^[[:space:]]*(Note:|#|;)'
                                  ; trailing prose / Note: / comment lines
                                  ; inside :success-check break sh -c
       ! awk '/^ :success-check/,/^[^ ]/' output/<slug>.edn | grep -qE 'setsid|fuser|nohup|docker (build|run)'
                                  ; live smoke-test commands forbidden in
                                  ; per-iteration :success-check
  6. Context-window budget check — must pass:
       OBJ_LEN=$(clojure -e '(count (:objective (clojure.edn/read-string (slurp \"output/<slug>.edn\"))))')
       test $OBJ_LEN -le 12000   ; under 12K chars total
     If this fails, the spec must be rewritten to lift detail into
     deliverable files; do not just trim prose to squeeze under the cap
     while leaving the structure unchanged.

  7. Tests-are-required check — :success-check must include a test
     invocation, not just file existence:
       awk '/^ :success-check/,/^[^ ]/' output/<slug>.edn \\
         | grep -qvE '^[[:space:]]*\"[[:space:]]*$' \\
         | grep -qE '(test|spec|check)[ -]|pytest|jest|vitest|mocha|cargo test|go test|rspec'
     If the project's test idiom is unusual, ensure the chain at minimum
     ends with a non-`test -f` command that exits 0 only when code is
     actually exercised.  A spec whose :success-check is purely
     `test -f` checks must be rejected.

  8. Print summary line:
       Spec written: output/<slug>.edn
       Provider: <provider>  Complexity: <complexity>
       Judge mode: <code|research>  Review threshold: <high|medium|low>
       Executor: <executor-model-id>  Supervisor: <supervisor-model-id>
       Phases: <N>  Visual phase: <kind|none>  Max-iterations: <N>
       Acceptance criteria: yes/no  Checkpoint-every: <N|n/a>

  === Reference spec — SIMPLE/MEDIUM (study the style — match it exactly) ===

  This is the model for SIMPLE/MEDIUM specs. For COMPLEX, also study
  graphs/options-trader/options-trader.edn in this repo — that file is
  the model for the depth and breadth a COMPLEX spec must reach.

  {:objective
   'Build a full-stack Kanban board application.

    === Phase 1: Skeleton ===
    Deliverables:
      package.json
      vite.config.ts
      server/index.ts
      db/schema.sql
      src/App.tsx (placeholder)
    - Vite + React + TypeScript project scaffold
    - Express + TypeScript server on port 3001
    - SQLite via better-sqlite3 at db/kanban.db
    - Single passing test in each of Vitest and Jest

    === Phase 2: Visual reference ===
    Deliverables: output/mockup.html
    A static HTML file showing the full Kanban UI: board with three columns
    (To Do, In Progress, Done), cards in each column, add-card and add-list
    controls, drag handle indicators, colour palette, and typography.
    No application logic — appearance only.  All subsequent UI phases must
    match this file.

    === Phase 3: React frontend ===
    Deliverables:
      src/App.tsx
      src/hooks/useKanban.ts
      src/components/Board.tsx
      src/components/List.tsx
      src/components/Card.tsx
      src/__tests__/kanban.test.tsx
      e2e/dnd.spec.ts
      playwright.config.ts
    Implement the UI from mockup.html.
    - Named lists side-by-side; add, rename, delete lists and cards
    - Drag-and-drop (reorder within a list, move between lists, reorder lists)
      via @dnd-kit; native HTML5 DnD only if the e2e below proves it works
    - Vitest + React Testing Library covering all CRUD operations
    - Playwright e2e (e2e/dnd.spec.ts): drag a card between lists, assert it
      moved and survives a reload — jsdom cannot simulate HTML5 drag, so this
      e2e is the only real proof DnD works

    === Phase 4: Backend + API ===
    Deliverables:
      server/routes/lists.ts
      server/routes/cards.ts
      server/__tests__/api.test.ts
    - REST: GET /api/health, GET /api/boards/:id,
      POST/PATCH/DELETE /api/lists, POST/PATCH/DELETE /api/cards
    - Jest + supertest for all endpoints

    === Phase 5: Docker deploy ===
    Deliverables:
      Dockerfile
      docker-compose.yml
      .dockerignore
    - Multi-stage Dockerfile; docker-compose.yml port 3001:3001

    === Phase 6: Documentation ===
    Deliverables:
      README.md
      CLAUDE.md
    - Summary, install, usage, manual smoke-test commands'

   :project-dir nil

   :acceptance-criteria
   '--- Phase 1 (Skeleton) ---
    Command:
      npm test
    Required:
      * Vitest exits 0 with at least one passing test.
      * Jest   exits 0 with at least one passing test.
      * db/kanban.db exists.

    --- Phase 2 (Visual reference) ---
    Command:
      test -f output/mockup.html
    Required:
      * mockup.html declares all three columns and the add-card / add-list controls.

    --- Phase 3 (React frontend) ---
    Command:
      npm run test:web && npm run test:e2e
    Required:
      * Unit tests cover create/rename/delete for both lists and cards.
      * Playwright e2e drags a card between lists and asserts it moved and
        persisted after reload — this, not a unit test, is the DnD proof.

    --- Phase 4 (Backend + API) ---
    Command:
      npm run test:api
    Required:
      * /api/health returns 200.
      * /api/lists and /api/cards each have CRUD coverage.

    --- Phase 5 (Docker deploy) ---
    Command:
      test -f Dockerfile && test -f docker-compose.yml && test -f .dockerignore
    Required:
      * All three artifacts present.
      * The Dockerfile is multi-stage (builder + runtime).
      * Live smoke-test (`podman-compose up --build -d && sleep 8 &&
        curl -sf http://localhost:3001/api/health && podman-compose down`)
        is documented in README.md as a manual verification step,
        intentionally NOT in :success-check.'

   :success-check
   'test -f package.json \\
    && test -f output/mockup.html \\
    && test -f src/App.tsx \\
    && test -f src/hooks/useKanban.ts \\
    && test -f server/routes/lists.ts \\
    && test -f server/routes/cards.ts \\
    && test -f e2e/dnd.spec.ts \\
    && test -f Dockerfile \\
    && test -f docker-compose.yml \\
    && test -f README.md \\
    && test -f CLAUDE.md \\
    && npm test \\
    && npm run build \\
    && npm run test:e2e'

   :constraints
   ['Work phase by phase — do not start phase N+1 until phase N tests pass'
    'Run npm test and npm run build at the end of every iteration'
    'Produce output/mockup.html in Phase 2 before writing any React components'
    'All UI phases after Phase 2 must reference mockup.html — do not restate design values in the objective'
    'Drag-and-drop is proven by the Playwright e2e (e2e/dnd.spec.ts) via npm run test:e2e, not jsdom unit tests; npm run test:e2e must install the browser headless if missing so the check is self-contained'
    'Keep all board state in a single useKanban hook']

   :anti-goals
   ['Do not verify drag-and-drop (or any browser-only behaviour) with jsdom/RTL unit tests alone, and do not mock DataTransfer or fire synthetic drag events to pass a unit test — DnD is done only when the Playwright e2e passes'
    'Do not install react-beautiful-dnd; @dnd-kit IS permitted for drag-and-drop'
    'Do not use an ORM — use better-sqlite3 with prepared statements'
    'Do not add dependencies beyond what is listed in the objective (plus @dnd-kit and @playwright/test)'
    'Do not leave TODO comments or unimplemented stubs'
    'Do not describe UI design in prose in the objective — mockup.html is the specification']

   :max-iterations      25
   :step-once?          false
   :generate-claude-md? false
   :judge-mode          :code
   :review-threshold    :high
   :fallback-model      'claude-haiku-4-5-20251001'
   :fallback-delay-ms   20000
   :executor-model-config   {:model-id 'claude-sonnet-4-6'}
   :supervisor-model-config {:model-id 'claude-sonnet-4-6'}}"

 :project-dir nil

 :success-check
 "test -d output &&
  SPEC=$(ls -t output/*.edn | head -1) &&
  test -n \"$SPEC\" &&
  clojure -e \"(clojure.edn/read-string (slurp \\\"$SPEC\\\"))\" &&
  grep -q '^ :objective'           $SPEC &&
  grep -q '^ :success-check'       $SPEC &&
  grep -q '^ :constraints'         $SPEC &&
  grep -q '^ :anti-goals'          $SPEC &&
  grep -q '^ :max-iterations'      $SPEC &&
  grep -q '^ :step-once?'          $SPEC &&
  grep -q '^ :generate-claude-md?' $SPEC &&
  grep -q '^ :judge-mode'          $SPEC &&
  grep -q '^ :review-threshold'    $SPEC &&
  grep -qE ':judge-mode +:(code|research)' $SPEC &&
  grep -qE ':review-threshold +:(none|low|medium|high)' $SPEC &&
  grep -qE ':step-once\\? +(true|false)' $SPEC &&
  grep -qE '^ :executor-model-config +\\{'   $SPEC &&
  grep -qE '^ :supervisor-model-config +\\{' $SPEC &&
  ! grep -q 'kill $SERVER_PID'        $SPEC &&
  ! grep -qE ':temperature|:max-tokens|:auth-token-env' $SPEC &&
  ! grep -qE 'color: *#|border-radius: *[0-9]|font-size: *[0-9]' $SPEC &&
  ! grep -q '<<<' $SPEC &&
  ! grep -q '^ :phases' $SPEC &&
  echo OK"

 :constraints
 ["Write the Phase 1 analysis to STDOUT before creating any file"
  "Model selection in Phase 2 must follow the table exactly — do not freestyle model IDs or use stale ones (e.g. claude-opus-4-5 is wrong; current Opus is claude-opus-4-7)"
  "The provider/agent keyword (:ollama :kimi :minimax :anthropic :pi) is stripped from the slug and the objective title but must drive model selection AND the :provider / :agent keys on the model configs"
  "All four model fields must be set — executor, supervisor, fallback-model, fallback-delay-ms — EXCEPT for :pi runs, which omit :fallback-model and :fallback-delay-ms (no automatic fallback is wired for the pi agent)"
  "Non-Anthropic claude-routed configs must include :provider per the Phase 2 dispatch table; pi-routed configs must include :agent :pi (and may optionally include :provider for a specific pi backend)"
  "Never emit :temperature, :max-tokens, or :auth-token-env — they are dead keys morpheus does not read; env-var names are hardcoded per provider in build-cmd+env"
  "Pick the visual artifact based on the kind of UI: web→mockup.html, TUI→mockup.txt, CLI→expected-session.txt, none→skip the visual phase"
  "The output spec's :objective must use single-quoted 'literals' or backtick `code` for any embedded code/JSON snippets — unescaped double-quotes terminate the outer EDN string and make the file unparseable"
  "MEDIUM and COMPLEX outputs must include an :acceptance-criteria key with per-phase shell gates"
  "Every output spec must declare :judge-mode (:code or :research) and :review-threshold (:low :medium or :high) so the downstream Wiggum run uses the right reviewer rubric"
  "Pick :judge-mode :research only when the deliverable is written analysis with citations (briefings, due diligence, literature reviews); pick :judge-mode :code for everything that produces source code, even if the project also has a writeup phase"
  "For :judge-mode :research default :review-threshold to :medium (citation discipline is brittle); for :judge-mode :code default to :high"
  "COMPLEX outputs must reach the depth of graphs/options-trader/options-trader.edn — every namespace, every column, every test fixture enumerated; thin sketches are a defect"
  "COMPLEX outputs must include :checkpoint-every (max-iterations / 5, ceil)"
  "The output file must parse as valid EDN via clojure.edn/read-string (strict) before declaring done"
  "Create the output/ directory if it does not exist"
  "Every phase header in the output spec must open with a `Deliverables:` block listing concrete file paths (or routes/functions for phases that flesh out an existing file) — the supervisor lifts this list straight into per-iteration expected_files"
  "The output spec's :success-check must be a single `&&`/`\\` continuation chain with no trailing prose, Note: blocks, or blank-line-separated comments — `sh -c` parses any non-continuation line as a separate command and the exit code becomes the last line (typically 127), so a poisoned check can never report success"
  "The output spec's :success-check is per-iteration: file existence (`test -f path`) plus an invocation of the project's native test runner — pick the idiomatic command for the chosen stack rather than prescribing one.  Never include `setsid`, `fuser`, `nohup`, port probes, `curl` to localhost, `docker build`, or `docker run` (Darwin lacks setsid/fuser, and live smoke-tests are too flaky for the per-iteration cycle)"
  "The output spec's :success-check MUST end with the project's test invocation, not just `test -f` checks.  Without a test command the executor can write syntactically broken code that still creates files and 'passes' verification, which is the failure mode the per-iteration check exists to prevent"
  "Cap the output spec's :objective at ~12000 characters total / ~2000 per phase.  The full :objective is embedded into iteration-1 CLAUDE.md verbatim and re-read by the supervisor every turn — overlong objectives starve the executor's context for the actual code it has to write.  Lift detail into deliverable files (mockup.html, schema.sql, docs/phase-N.md) rather than inlining"
  "Live server smoke-tests, `docker build`, and `docker run` belong in a Phase-N acceptance criterion AND a documented manual verification block in README.md — not in :success-check"
  "Inside the output spec's :objective string, escape multi-line shell continuations as `\\\\` (parsed to a literal `\\`); a bare `\\` followed by a newline is not a valid EDN escape and `clojure.edn/read-string` will reject the file"]

 :anti-goals
 ["Do not invent model IDs not listed in the Phase 2 table"
  "Do not include the provider/agent keyword (:ollama :kimi :minimax :anthropic :pi) in the output spec slug or title"
  "Do not produce anything other than the analysis and the single output EDN file"
  "Do not write placeholder values — every field must be fully filled in"
  "Do not wrap the EDN in markdown code fences — the file must be raw EDN"
  "Do not set :generate-claude-md? to true — this is a single-output spec, not a full project"
  "Do not write CSS, colour values, or layout descriptions in the output spec :objective — that belongs in the visual artifact"
  "Do not produce a thin SIMPLE-shaped spec when the description is COMPLEX (multi-service, persistent storage, agent integration, etc.); under-detailing is the most common failure mode"
  "Do not emit :temperature, :max-tokens, or :auth-token-env keys — none of them are read by morpheus"
  "Do not list :glm, :openrouter, or :custom as a provider — they are not implemented in claude_code_agent.clj/build-cmd+env (only :ollama, :kimi, :minimax, and the default :anthropic are wired for the claude agent; pi has its own provider list)"
  "Do not omit :judge-mode or :review-threshold from the output spec — defaulting silently to :code is wrong for research-style deliverables and the user has no way to discover the mismatch from the generated file alone"
  "Do not pick :judge-mode :spec for the output — that mode is for reviewing specs themselves, not for downstream runs of those specs"
  "Do not emit `setsid` or `fuser` anywhere in the output spec — both are Linux-only and the user runs morpheus on macOS where these commands fail with exit 127"
  "Do not append a `Note:` block, explanatory paragraph, or any non-`&&`/non-`\\` line to :success-check — this was a real defect that left a generated spec's verification permanently broken"
  "Do not embed verbatim file bodies of more than ~50 lines in :objective — emit them as separate Phase-1 deliverable files instead; large embedded bodies bloat iteration-1 CLAUDE.md and the supervisor's per-iteration prompt"
  "Do not produce an :objective longer than 12000 characters — it is the single biggest driver of context bloat across the whole downstream run"
  "Do not produce a :success-check whose only commands are `test -f` checks — it must invoke the project's tests so syntactically broken code is caught"
  "Do not prescribe a specific test runner in the schema/rules sections of generator output — pick the idiom of the chosen stack (the executor knows them all) and only mention concrete commands inside the generated spec itself"
  "Do not emit a :phases key in the output spec — all phase detail belongs inside the :objective string as === Phase N: name === headings with Deliverables: blocks; a separate :phases key is unused by wiggum and will fail validation"]

 :max-iterations      6
 :step-once?          false
 :generate-claude-md? false

 ;; Use the spec-aware judge rubric (parse safety, schema completeness,
 ;; phase consistency, depth match) instead of the default code-reviewer
 ;; rubric. Pause whenever the judge finds medium+ defects so the user
 ;; can patch the spec before downstream Wiggum loops grind against it.
 :judge-mode          :spec
 :review-threshold    :medium

 :fallback-model    "claude-haiku-4-5-20251001"
 :fallback-delay-ms 20000

 :executor-model-config
 {:model-id "claude-sonnet-4-6"}

 :supervisor-model-config
 {:model-id "claude-sonnet-4-6"}}
